{
  "name": "agentic-sop-to-work",
  "owner": {
    "name": "s0912758806p",
    "url": "https://github.com/s0912758806p"
  },
  "description": "Loop Engineering for Claude Code agent loops — deterministic bounded termination (stall + budget), observable health, and bounded state — and Graph Engineering for when one loop isn't enough: typed handoff edges, unique field ownership, bounded back-edges, and static topology validation. Ships agentic-sop-kit (human SOP → controlled loop or bounded graph) plus cross-domain review companions: Second Opinion (honesty reviewer), alcoa-guard (ALCOA+ data-integrity), plugin-forge (plugin linter/scaffolder).",
  "plugins": [
    {
      "name": "agentic-sop-kit",
      "source": "./plugins/agentic-sop-kit",
      "description": "Human SOP → a controlled agentic loop, or a bounded graph of them: typed handoff edges, one declaring writer per state field, bounded back-edges, and a static topology gate that refuses an illegal graph before it runs. Methodology skills + /sop-flow + a project-scoped regression Stop-hook + the portable kit."
    },
    {
      "name": "second-opinion-honesty",
      "source": "./plugins/second-opinion-honesty",
      "description": "Independent adversarial honesty reviewer: scrutinizes a DRAFT output's claims-vs-evidence (no fabrication / overreach / invented IDs) before a human approves. Snaps onto agentic-sop-kit run dirs (FULL, uses the trace) or reviews any plain document (DEGRADED). Deterministic checks + a capped advisory LLM pass; the report is a DRAFT — the human owns the verdict."
    },
    {
      "name": "alcoa-guard",
      "source": "./plugins/alcoa-guard",
      "description": "Deterministic ALCOA+ data-integrity linter: checks Attributable / Contemporaneous / Complete / Accurate / Consistent in code and surfaces the human-judgment slice as a 【待補】 checklist it never auto-concludes. DEGRADED lints any CSV/JSON record against a declared contract; FULL snaps onto an agentic-sop-kit run dir. The report is a DRAFT — the human owns the verdict. Pure stdlib."
    },
    {
      "name": "plugin-forge",
      "source": "./plugins/plugin-forge",
      "description": "Claude Code plugin linter + scaffolder: `lint` validates a plugin or whole marketplace against a house grammar (a strict superset of the manifest/frontmatter checks plus stdlib-only, hook-protocol, and test-harness invariants); `scaffold` generates a grammar-conformant plugin skeleton. Pure stdlib; self-hosting; standalone on any plugin."
    }
  ]
}
